import React from 'react';
import Layout from 'components/utils/layout';
import { Panel } from 'components/ui';

export default () => {
  return (
    <Layout>
      <Panel>
        
      </Panel>
    </Layout>
  )
}